class SomeClass(object):
    def add(x: int, y: int) -> int:
        x = [ i + 1 for i in range(1, 10) if i % 2 == 0 ]
        ("hello", True)

        if True:
            return False

        return x + y
